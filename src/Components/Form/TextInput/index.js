import React from "react"; 

//(nome, email, data de nascimento, cpf, cep, endereço, numero, senha);

function TextInput(props) {
  return (
    <>
      <label htmlFor={''} />
      <input type={''} name="username" value={''} onChange={''}/>
    </>
  )
}

export default TextInput